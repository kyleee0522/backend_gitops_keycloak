apiVersion: v1	
kind: Service	
metadata:	
  name: backend-keycloak
  labels:
    app: backend-keycloak
spec:	
  ports:	
  - port: 9080	
    targetPort: 8080
    name: http
  selector:	
    app: backend-keycloak
  type: ClusterIP
